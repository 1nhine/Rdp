name: Linux RDP Runner

on:
  workflow_dispatch:

jobs:
  linux-session:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Setup LXDE Desktop and XRDP
        run: |
          sudo apt update
          # Cai dat LXDE desktop va xrdp (khong bi loi failsafe session)
          sudo DEBIAN_FRONTEND=noninteractive apt install -y lxde xrdp

          # 1. Tao user rdpuser voi mat khau Komura@123
          sudo useradd -m -s /bin/bash rdpuser || true
          echo "rdpuser:Komura@123" | sudo chpasswd
          sudo usermod -aG sudo rdpuser
          sudo adduser xrdp ssl-cert

          # 2. Cau hinh session khoi dong thang vao LXDE
          echo "startlxde" | sudo tee /home/rdpuser/.xsession
          sudo chown rdpuser:rdpuser /home/rdpuser/.xsession
          sudo chmod +x /home/rdpuser/.xsession

          # 3. Khoi dong lai dich vu XRDP
          sudo systemctl restart xrdp

      - name: Connect Tailscale and Keep Alive
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
        run: |
          if [ -z "$TAILSCALE_AUTHKEY" ]; then
            echo "Vui long kiem tra Secret TAILSCALE_AUTHKEY trong Settings!"
            exit 1
          fi

          curl -fsSL https://tailscale.com/install.sh | sh
          sudo tailscale up --authkey=$TAILSCALE_AUTHKEY --hostname="github-linux-rdp" --accept-routes

          TAILSCALE_IP=$(tailscale ip -4)

          echo "=========================================="
          echo "      THONG TIN DANG NHAP WINDOWS APP     "
          echo "=========================================="
          echo "PC Name (IP) : $TAILSCALE_IP"
          echo "Port         : 3389"
          echo "Username     : rdpuser"
          echo "Password     : Komura@123"
          echo "=========================================="

          MAX_SECONDS=21000
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_SECONDS ]; do
            sleep 60
            ELAPSED=$((ELAPSED + 60))
            REMAINING=$(( (MAX_SECONDS - ELAPSED) / 60 ))
            echo "Runner is active... Remaining: $REMAINING minutes."
          done
